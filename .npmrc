# npm >= 12 blocks git dependencies by default (allow-git=none).
# This project depends on two forks hosted on GitHub (proj4leaflet, mapml-extension),
# so allow git specs declared in this package.json only.
allow-git=root
